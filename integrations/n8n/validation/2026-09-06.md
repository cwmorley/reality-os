# Synthetic planner run: 2026-09-06

**Result: 16 tests passed, 0 failed.** This validates only deterministic classification for the preserved synthetic cases and execution of the generated Code-node JavaScript inside Node's VM. It does not validate n8n import, an agent runner, persistence, runtime permissions, the cooperative lock, or real canonical writeback.

## Setup and preserved evidence

- Scope: optional n8n adapter contract v0.1, on Windows with Node.js v24.20.0.
- Revision: the source digests below identify the exact tested implementation and contract. The containing Git commit supplies the repository revision once published.
- Input: only fictional metadata in `fixtures.mjs` and explicit alterations in `planner.test.mjs`. No private vault, credentials, models, agents, or external services were loaded.
- Isolation/control: one fresh Node test process; each unit case receives a fresh snapshot. Tests directly compare decisions with stated expectations. The workflow test runs its embedded code in a fresh VM context and compares it with the reference planner and four independently stated expected action names.
- Measurement: Node's built-in test runner, assertions, and pass/fail counts. Context-token measurement is not applicable; no model call occurred.
- Full output: [`2026-09-06.tap`](./2026-09-06.tap).

Reproduce from the repository root:

```sh
node integrations/n8n/build-workflow.mjs
node --test --test-reporter=tap integrations/n8n/planner.test.mjs
```

SHA-256 digests of the tested files (LF source bytes):

| File, relative to `integrations/n8n/` | SHA-256 |
|---|---|
| `planner.mjs` | `1765E43C281E3848167B43940154ECE9014781D6BDA7157B7DCE93EFA82BC10B` |
| `planner.test.mjs` | `1B2BB8A60B49FA5FF816A13C552DF0B196C80BDE7C6FE2505513C54205C0CAC7` |
| `workflow.dry-run.json` | `6C6228C8B5F7EFEC1C600F66A8F43324AE240A07A213D4C1C01AF5D8BB5DC1A6` |
| `PROTOCOL.md` | `453E540E51C25E4D6D4B7ECB5CFFE2730D8231A90B5ADBADE123F95DDC2E64FA` |

## Observed behavior and reassessment

New ready versions produce review suggestions. Every supported receipt disposition suppresses that exact version, including a deferral. Updated versions remain eligible, while rollback and conflicting metadata at the same update time block. Withdrawal and supersession request a terminal receipt rather than a reopened question. Answer-like states still require a reconciler's review. Outstanding jobs prevent duplicate dispatch suggestions; timeout and process completion without receipts require reconciliation.

Unregistered sources block. Candidate-supplied recipient text cannot change the configured recipient. Disabled routes, incomplete items, and off-hours prevent new review suggestions. Invalid and duplicate input aborts the entire batch. The generated workflow contains only a manual trigger and a Code node and produces the four documented outputs.

During author self-review, a possible gap was identified: changing the configured recipient could bypass an earlier recipient's outstanding job. The planner was changed to check outstanding work across recipients and a specific regression test was added before this recorded run. That test passes for running, uncertain, and queued earlier jobs. This was an author finding, not an independently observed deployment failure or independent review.

Disposition: pass for these synthetic cases. No core Reality OS validation row was promoted. Live deployment remains untested under N1–N6. Independent diff review and a cold-reader comprehension check remain pending before merge; do not relabel this author's inspection as either one.
